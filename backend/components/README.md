# Components

Reusable backend building blocks: controllers, services, middleware, and
data models used by the routers. Organize by domain as they're added
(e.g. `auth/`, `orders/`, `payments/`, `delivery/`).
