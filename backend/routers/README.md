# Routers

Route definitions for each domain (e.g. `auth.routes.js`, `customer.routes.js`,
`restaurant.routes.js`, `delivery.routes.js`, `admin.routes.js`) live here.
Each router only defines endpoints and delegates logic to `../components`.
